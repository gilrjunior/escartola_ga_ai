{
  "builds": [
    { "src": "config/wsgi.py", "use": "@vercel/python", "config": { "runtime": "python3.12" } }
  ],
  "installCommand": "python3 -m pip install --upgrade pip && python3 -m pip install --no-compile --no-cache-dir -r requirements.txt",
  "routes": [
    { "src": "/assets/(.*)", "dest": "/config/public/$1", "headers": { "cache-control": "public, max-age=31536000, immutable" } },
    { "src": "/favicon\\.(ico|png)", "dest": "/config/public/logo.png", "headers": { "cache-control": "public, max-age=31536000, immutable" } },
    { "src": "/static/(.*)", "dest": "/staticfiles/$1" },
    { "src": "/(.*)", "dest": "config/wsgi.py" }
  ]
}
